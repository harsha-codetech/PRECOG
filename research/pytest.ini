[pytest]
pythonpath = .
testpaths = tests
# A globally installed web3 ships a broken pytest_ethereum plugin that fails on import.
# It is unrelated to this project, so it is disabled rather than worked around.
addopts = -q -p no:ethereum
